# Tab

A single tab — only meaningful when projected inside `<gbt-tabs>`.

**Selector**: `gbt-tab`

## Inputs

| Input   | Type     | Role                     |
| ------- | -------- | ------------------------ |
| `label` | `string` | Required. Trigger label. |

See [`../tabs/README.md`](../tabs/README.md).
